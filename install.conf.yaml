- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.exports: bash/autocompletion.zsh
    ~/.exports: bash/exports.zsh
    ~/.zshrc: bash/zshrc.zsh
    ~/.p10k.zsh: bash/p10k.zsh

- shell:
  - [git submodule update --init --recursive, Installing submodules]